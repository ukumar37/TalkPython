def load(name):  # load journal
    # todo: populate from file if it exists
    return []


def save(name, journal_data):  # save journal along with the data
    pass


def add_entry(text, journal_data):
    journal_data.append(text)